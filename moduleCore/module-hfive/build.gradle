apply from: "../../module.build.gradle"
android {
    defaultConfig {
        if (isBuildModule.toBoolean()) {
            applicationId "com.hfive"
        }
    }

}

dependencies {
    api project(':library-base')
}
